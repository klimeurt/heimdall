{{- if .Values.coordinator.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "heimdall.coordinator.name" . }}
  labels:
    {{- include "heimdall.labels" . | nindent 4 }}
    {{- include "heimdall.componentLabels" "coordinator" | nindent 4 }}
spec:
  replicas: {{ .Values.coordinator.replicaCount }}
  selector:
    matchLabels:
      {{- include "heimdall.selectorLabels" . | nindent 6 }}
      {{- include "heimdall.componentLabels" "coordinator" | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        {{- include "heimdall.selectorLabels" . | nindent 8 }}
        {{- include "heimdall.componentLabels" "coordinator" | nindent 8 }}
    spec:
      {{- include "heimdall.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "heimdall.serviceAccountName" . }}
      securityContext:
        {{- include "heimdall.podSecurityContext" . | nindent 8 }}
      initContainers:
      - name: wait-for-redis
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z {{ include "heimdall.fullname" . }}-redis-master {{ .Values.redis.master.service.ports.redis }}; do echo waiting for redis; sleep 2; done;']
      containers:
      - name: coordinator
        securityContext:
          {{- include "heimdall.containerSecurityContext" . | nindent 10 }}
        image: "{{ .Values.global.imageRegistry }}/{{ .Values.coordinator.image.repository }}:{{ .Values.global.imageTag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.coordinator.image.pullPolicy }}
        envFrom:
        - configMapRef:
            name: {{ include "heimdall.fullname" . }}-config
        resources:
          {{- toYaml .Values.coordinator.resources | nindent 10 }}
      {{- with .Values.coordinator.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.coordinator.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.coordinator.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}